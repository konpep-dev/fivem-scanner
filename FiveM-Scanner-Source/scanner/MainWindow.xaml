<Window x:Class="FiveMScanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="&lt;/async&gt;" 
        Height="318" 
        Width="560"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#111111"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFFFFF"/>
                                <Setter Property="Foreground" Value="#111111"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="#666666"/>
                                <Setter Property="BorderBrush" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PIN TextBox Style -->
        <Style x:Key="PinTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Padding="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main Container with Border -->
    <Border Background="#111111" BorderBrush="#333333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Background Particles Canvas -->
            <Canvas x:Name="ParticlesCanvas" Grid.Row="0" IsHitTestVisible="False" Opacity="0.3"/>

            <!-- Close Button (Top Right) -->
            <StackPanel Orientation="Horizontal" 
                       Grid.Row="0"
                       HorizontalAlignment="Right" 
                       VerticalAlignment="Top"
                       Margin="0,6,6,0"
                       Background="Transparent"
                       MouseLeftButtonDown="CloseButton_Click"
                       Cursor="Hand">
                <Ellipse Width="8" Height="8" Fill="#333333" Stroke="#555555" StrokeThickness="1" Margin="2"/>
                <Ellipse Width="8" Height="8" Fill="#333333" Stroke="#555555" StrokeThickness="1" Margin="2"/>
                <Ellipse Width="8" Height="8" Fill="#333333" Stroke="#555555" StrokeThickness="1" Margin="2"/>
            </StackPanel>

            <!-- Main Content Area -->
            <Grid Grid.Row="0" x:Name="ContentGrid">
                <!-- Initial State -->
                <StackPanel x:Name="InitialPanel" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Visibility="Visible">
                    
                    <!-- Logo: </async> -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                        <TextBlock Text="&lt;/" FontFamily="Consolas" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF"/>
                        <TextBlock Text="async" FontFamily="Consolas" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF"/>
                        <TextBlock Text="&gt;" FontFamily="Consolas" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF"/>
                    </StackPanel>

                    <!-- PIN Entry (Initially Hidden) -->
                    <StackPanel x:Name="PinEntryPanel" Visibility="Collapsed" Width="240">
                        <TextBlock Text="Enter the 6-digit PIN to begin." 
                                  FontFamily="Consolas" 
                                  FontSize="11" 
                                  Foreground="#888888"
                                  TextAlignment="Center"
                                  Margin="0,0,0,10"/>
                        
                        <TextBox x:Name="PinTextBox"
                                Style="{StaticResource PinTextBox}"
                                MaxLength="6"
                                TextAlignment="Center"
                                PreviewTextInput="PinTextBox_PreviewTextInput"
                                TextChanged="PinTextBox_TextChanged"/>
                        
                        <TextBlock x:Name="PinErrorText"
                                  FontFamily="Consolas"
                                  FontSize="10"
                                  Foreground="#FF4444"
                                  TextAlignment="Center"
                                  Margin="0,6,0,0"
                                  Visibility="Collapsed"/>
                        
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Button Grid.Column="0" 
                                   Content="CANCEL" 
                                   Style="{StaticResource PrimaryButton}"
                                   Background="Transparent"
                                   BorderBrush="#444444"
                                   Foreground="#CCCCCC"
                                   Click="CancelPin_Click"/>
                            
                            <Button x:Name="SubmitPinButton"
                                   Grid.Column="2" 
                                   Content="START" 
                                   Style="{StaticResource PrimaryButton}"
                                   Click="SubmitPin_Click"
                                   IsEnabled="False"/>
                        </Grid>
                    </StackPanel>

                    <!-- Initiate Scan Button -->
                    <Button x:Name="InitiateScanButton"
                           Content="INITIATE SCAN" 
                           Style="{StaticResource PrimaryButton}"
                           Width="180"
                           Click="InitiateScan_Click"/>
                    
                    <!-- ToS and FAQ Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                        <Button x:Name="TosButton"
                               Content="t.o.s" 
                               FontFamily="Consolas"
                               FontSize="9"
                               Foreground="#666666"
                               Background="Transparent"
                               BorderThickness="0"
                               Padding="4"
                               Margin="0,0,8,0"
                               Cursor="Hand"
                               Click="TosButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Foreground" Value="#999999"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        
                        <TextBlock Text="•" 
                                  FontFamily="Consolas" 
                                  FontSize="9" 
                                  Foreground="#333333" 
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0"/>
                        
                        <Button x:Name="FaqButton"
                               Content="f.a.q" 
                               FontFamily="Consolas"
                               FontSize="9"
                               Foreground="#666666"
                               Background="Transparent"
                               BorderThickness="0"
                               Padding="4"
                               Cursor="Hand"
                               Click="FaqButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Foreground" Value="#999999"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </StackPanel>

                <!-- Scanning State -->
                <StackPanel x:Name="ScanningPanel" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Visibility="Collapsed"
                           Width="350">
                    
                    <TextBlock Text="ANALYZING SYSTEM" 
                              FontFamily="Consolas" 
                              FontSize="16" 
                              FontWeight="Medium"
                              Foreground="#F0F0F0"
                              TextAlignment="Center"
                              Margin="0,0,0,15"/>
                    
                    <!-- Progress Bar -->
                    <Border Height="3" Background="#222222" Margin="0,0,0,8">
                        <Border x:Name="ProgressBar" 
                               Background="#FFFFFF" 
                               HorizontalAlignment="Left"
                               Width="0"/>
                    </Border>
                    
                    <!-- Status Text -->
                    <TextBlock x:Name="StatusText"
                              Text="INITIALIZING..."
                              FontFamily="Consolas"
                              FontSize="11"
                              Foreground="#888888"
                              TextAlignment="Center"
                              Margin="0,0,0,5"
                              TextWrapping="Wrap"/>
                    
                    <!-- Progress Percentage -->
                    <TextBlock x:Name="ProgressText"
                              Text="0%"
                              FontFamily="Consolas"
                              FontSize="24"
                              FontWeight="Bold"
                              Foreground="#FFFFFF"
                              TextAlignment="Center"/>
                </StackPanel>

                <!-- Complete State -->
                <StackPanel x:Name="CompletePanel" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Visibility="Collapsed">
                    
                    <TextBlock Text="&lt;/&gt;" 
                              FontFamily="Consolas" 
                              FontSize="40" 
                              FontWeight="Bold"
                              Foreground="#FFFFFF"
                              TextAlignment="Center"
                              Margin="0,0,0,12"/>
                    
                    <TextBlock Text="SCAN COMPLETE" 
                              FontFamily="Consolas" 
                              FontSize="16" 
                              FontWeight="Medium"
                              Foreground="#F0F0F0"
                              TextAlignment="Center"
                              Margin="0,0,0,6"/>
                    
                    <TextBlock x:Name="CompleteMessage"
                              Text="Report submitted successfully."
                              FontFamily="Consolas"
                              FontSize="11"
                              Foreground="#888888"
                              TextAlignment="Center"
                              TextWrapping="Wrap"
                              MaxWidth="320"/>
                </StackPanel>
            </Grid>

            <!-- Footer -->
            <Border Grid.Row="1" 
                   Background="#111111" 
                   BorderBrush="#333333" 
                   BorderThickness="0,1,0,0"
                   Padding="0,4">
                <TextBlock Text="&lt;/async&gt; v2.0 (FiveM Engine Edition)" 
                          FontFamily="Consolas" 
                          FontSize="9" 
                          Foreground="#888888"
                          Opacity="0.5"
                          TextAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Window>
