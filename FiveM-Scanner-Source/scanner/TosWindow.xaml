<Window x:Class="FiveMScanner.TosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Terms of Service" 
        Height="600" 
        Width="700"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <!-- Custom ScrollBar Style -->
        <Style x:Key="CustomScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Background" Value="#444444"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border Background="{TemplateBinding Background}" 
                                                                   CornerRadius="4"
                                                                   Margin="1"/>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#666666"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0"/>
                            <ScrollBar Grid.Column="1" 
                                      x:Name="PART_VerticalScrollBar"
                                      Style="{StaticResource CustomScrollBar}"
                                      Value="{TemplateBinding VerticalOffset}"
                                      Maximum="{TemplateBinding ScrollableHeight}"
                                      ViewportSize="{TemplateBinding ViewportHeight}"
                                      Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TosButton" TargetType="Button">
            <Setter Property="Background" Value="#111111"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFFFFF"/>
                                <Setter Property="Foreground" Value="#111111"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#111111" BorderBrush="#333333" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Background Particles Canvas -->
            <Canvas x:Name="ParticlesCanvas" Grid.Row="0" Grid.RowSpan="3" IsHitTestVisible="False" Opacity="0.2"/>

            <!-- Header -->
            <Border Grid.Row="0" Background="#111111" BorderBrush="#333333" BorderThickness="0,0,0,1" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock Text="&lt;/" FontFamily="Consolas" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF"/>
                        <TextBlock Text="async" FontFamily="Consolas" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF"/>
                        <TextBlock Text="&gt;" FontFamily="Consolas" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF"/>
                        <TextBlock Text=" Terms of Service" FontFamily="Consolas" FontSize="14" Foreground="#888888" VerticalAlignment="Center" Margin="12,0,0,0"/>
                    </StackPanel>
                    
                    <!-- Close Button -->
                    <StackPanel Orientation="Horizontal" 
                               HorizontalAlignment="Right" 
                               VerticalAlignment="Center"
                               Background="Transparent"
                               MouseLeftButtonDown="CloseButton_Click"
                               Cursor="Hand">
                        <Ellipse Width="8" Height="8" Fill="#333333" Stroke="#555555" StrokeThickness="1" Margin="2"/>
                        <Ellipse Width="8" Height="8" Fill="#333333" Stroke="#555555" StrokeThickness="1" Margin="2"/>
                        <Ellipse Width="8" Height="8" Fill="#333333" Stroke="#555555" StrokeThickness="1" Margin="2"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" 
                         Style="{StaticResource CustomScrollViewer}"
                         VerticalScrollBarVisibility="Auto" 
                         Padding="30,20,20,20">
                <TextBlock x:Name="TosTextBlock"
                          FontFamily="Consolas"
                          FontSize="11"
                          Foreground="#CCCCCC"
                          LineHeight="18"
                          TextWrapping="Wrap"
                          Text=""/>
            </ScrollViewer>

            <!-- Footer with Buttons -->
            <Border Grid.Row="2" Background="#111111" BorderBrush="#333333" BorderThickness="0,1,0,0" Padding="20,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Content="🔊 READ ALOUD" 
                           Style="{StaticResource TosButton}"
                           Click="ReadAloud_Click"
                           Margin="0,0,10,0"/>
                    <Button Content="CLOSE" 
                           Style="{StaticResource TosButton}"
                           Background="Transparent"
                           BorderBrush="#444444"
                           Foreground="#CCCCCC"
                           Click="Close_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
